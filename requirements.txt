# Flask 3.1+ needed for Python 3.14 (pkgutil.get_loader removed); use 2.3.x on older Python if required
Flask>=3.1.0,<4
Flask-SQLAlchemy==3.1.1
gunicorn==20.1.0
# 3.11.x may require building from source on Python 3.14+; use a release with wheels
aiohttp>=3.11.7
aiofiles==24.1.0
alembic==1.14.0
attrs==24.2.0
bcrypt==4.2.1
blinker==1.9.0