Follow `AGENTS.md` in the project root. It is the single source of truth for
all AI agents (rules, guardrails, MCP usage, sub-agent coordination, Memory
Bank protocol). The legacy `.antigravityrules` file is now a pointer to
`AGENTS.md` and exists only for backward compatibility — read `AGENTS.md`
directly.
