# 🏛️ Архитектура системы - Shadow Hockey League v2

**Версия:** 2.6.0
**Дата обновления:** 13 апреля 2026 г. (Points Calculator fix)
**Статус:** ✅ Production / ⚠️ Admin Maintenance

---

## 🎯 1. Бизнес-цели (Business Goals)

- **BG-01**: Централизованное управление лигами, сезонами и достижениями хоккеистов.
- **BG-02**: Безошибочное управление данными через админ-панель (0 критических 500-ошибок).
- **BG-03**: Контролируемая эксплуатация в production (мониторинг health/metrics).
- **BG-04**: Высокое качество через автоматизацию тестов критических путей.

---

## 🏗️ 2. Архитектурная схема

```
┌─────────────────┐
│     Client      │
│   (Web Browser) │
└────────┬────────┘
         │ HTTPS
         ▼
┌─────────────────────────────────┐
│   Nginx (Reverse Proxy + SSL)   │
│   - Port 443 (HTTPS)            │
│   - Port 80 (HTTP → HTTPS)      │
│   - Static files (/static)      │
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│   Gunicorn (WSGI Server)        │
│   - 4 workers                   │
│   - Port 8000 (internal)        │
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│   Flask Application             │
│   - Application Factory pattern │
│   - Blueprints для маршрутов    │
│   - Services для бизнес-логики  │
└───┬─────────┬─────────┬─────────┘
    │         │         │
    ▼         ▼         ▼
┌───────┐ ┌──────┐ ┌────────┐
│SQLite │ │Redis │ │Promethe│
│  DB   │ │Cache │ │  us    │
└───────┘ └──────┘ └────────┘
```

---

## 📦 3. Технологический стек

| Уровень            | Технология               | Назначение         |
| ------------------ | ------------------------ | ------------------ |
| **Backend**        | Python 3.10+, Flask 3.1+ | Основной стек      |
| **ORM**            | SQLAlchemy 2.0+, Alembic | БД и миграции      |
| **Caching**        | Redis 6.0+               | Ускорение и сессии |
| **Admin**          | Flask-Admin              | Панель управления  |
| **Infrastructure** | Nginx, Gunicorn, Ubuntu  | Хостинг            |

---

## � 4. Функциональное состояние (Status 2026-04-12)

### 4.1 Публичная часть (Public UI)

| ID     | Требование                              | Статус |
| ------ | --------------------------------------- | ------ |
| FR-001 | Leaderboard с сортировкой по очкам      | ✅     |
| FR-002 | Детализация расчёта (base × multiplier) | ✅     |
| FR-004 | Кэширование + инвалидация               | ✅     |

### 4.2 Админ-панель (Admin UI)

| ID     | Требование                                   | Статус                |
| ------ | -------------------------------------------- | --------------------- |
| FR-101 | CRUD: Country, Manager, AchievementType      | ✅                    |
| FR-102 | Achievement CRUD (через Manager)             | ⚠️ Runtime ошибки     |
| FR-103 | Reactive Points Calculator (AchievementType) | ✅ Fixed (2026-04-13) |
| FR-105 | Логин/Логаут стабильность                    | ❌ (Баг Logout route) |
| FR-106 | Dashboard админки                            | ❌ (Битые ссылки)     |

---

## � 5. API Endpoints (Admin)

- `GET /admin/api/leagues` — List active leagues.
- `GET /admin/api/seasons?league_id=X` — List seasons for league.
- `GET /admin/api/calculate-points` — Server-side points validation.

---

## �🔒 6. Безопасность и Мониторинг

- **Auth**: Flask-Login (Session) + API Key (Hash).
- **Health**: `/health` (Redis, DB, Managers count).
- **Metrics**: `/metrics` (Prometheus format).
- **Backups**: Daily (03:00 UTC) + Pre-deploy.

---

## ⚠️ 6. Известные проблемы (Known Issues)

1. **BUG-A1**: Битый endpoint в Dashboard (`achievement.index_view`).
2. **BUG-A2**: Ошибка 500 на `GET /admin/api/managers/{id}/achievements`.
3. **BUG-A3**: Редирект на несуществующий `admin_login` при выходе.
4. **BUG-A5**: Рассинхрон документации и кода (исправляется в рамках текущего аудита).

---

_Generated by ANALYST agent on 2026-04-13_

### 📝 Последние изменения

- **FR-103**: Fixed Points Calculator - добавлен реактивный пересчет очков, серверная валидация, поддержка лиг 2.1/2.2.
