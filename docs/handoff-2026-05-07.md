# Handoff — 2026-05-07 — TIK-72 round-2 (TIK-74…TIK-80) ready for merge

> **Author:** Devin session `https://app.devin.ai/sessions/7b61b28494e040cfb7923168919a1979`
> **Status:** All code complete. CI green. E2E green. Awaiting human-driven merge.
>
> This document exists so the next AI / engineer can pick up cleanly without
> re-reading the entire session transcript. Read this first, then `docs/progress.md`
> + `AGENTS.md` per Memory Bank Protocol.

---

## TL;DR

- **PR #79** ([link](https://github.com/amatjkay/shadow-hockey-league_v2/pull/79)) — branch `devin/1778008520-post-qa-fixes` → `staging`. Mergeable.
- **PR #78** ([link](https://github.com/amatjkay/shadow-hockey-league_v2/pull/78)) — branch `staging` → `main`. Will pick up #79 once merged.
- **No open code work.** Nothing is half-done.
- **Merge order:** PR #79 first, then PR #78.

---

## What was done in this round

Round-2 QA on top of the TIK-59 batch (PR #76 → staging). All of these are children of **TIK-72** (Round-2 QA epic):

| Ticket | One-line summary |
| :--- | :--- |
| TIK-74 | Hide `Войти` button for anonymous users (single-owner-admin model — `s3ifer` logs in via `/admin/login/` directly). |
| TIK-75 | Re-style season selector: pill-shaped dark control with cyan caret, glass background, focus ring. |
| TIK-76 | Empty Season dropdown for League 2.1/2.2 in the admin Add-Achievement modal (root cause: `Season.start_year`/`end_year` NULL after legacy schema add → silently filtered out by VR-004). Alembic migration `b5f2a8e1c34d` backfills both columns by parsing `Season.code`. |
| TIK-77 | `404 GET /static/img/cups/r3.svg` after admin saves Round-3 / Round-1 / Best-Regular. Root cause: `AchievementType.get_icon_url()` synthesised `/static/img/cups/{code.lower()}.svg`, but canonical files are `hockey-sticks-and-puck.svg` + `best-reg.svg`. Fix: fallback to `default.svg` + populate `AchievementType.icon_path` in seed + migration `c7d3e2f1a8b9` backfills + rewrites stale rows. |
| TIK-78 | Concise tooltip. Root cause: `AchievementModelView.on_model_change` stored `Achievement.title = "{type.name} {league.name} {season.name}"` while `to_html` already wraps with `Shadow … s{season}` → `Shadow 2.1 league Round 3 League 2.1 Season 2025/26 s25/26`. Fix: title now stores `type.name` only. Migration `d8e4f3a2b9c0` rewrites legacy rows. |
| TIK-77/78 AJAX fix | `blueprints/admin_api/achievements.py::_create_achievement_for_manager` and `_process_bulk_create_manager` (used by the modal's AJAX path) duplicated icon-path + title logic and bypassed the TIK-77/78 fixes. Patched both to use `ach_type.get_icon_url()` and `title=ach_type.name`. Regression test `test_api_bulk_add_uses_canonical_icon_path_and_short_title`. |
| TIK-79 | Admin-readability. Root cause: Flask-Admin called `__repr__` for FK rendering → users saw `<Achievement 1/1/2>`, `<Manager Felix>`, `<Country Belarus (BLR)>`. Fix: explicit `__str__` on `Manager`, `AchievementType`, `League` (subleagues show code, e.g. `League 2.1 (2.1)`), `Season`, `Country`, `Achievement` (composite human label `<Manager> — <Type> (<League>, <Season>)`). Audit-log `target_id` resolved by registry-based `_format_target_link` to `<str(row)> — #<id>` with `<Model> #<id> (deleted)` fallback. `__repr__` is **kept** unchanged for logs and pdb. Tests in `tests/test_model_str.py`. |
| TIK-80 | Admin polish. Search across FK relations (typing `Russia` returns Russian managers via `country.name`); FK-based filter sidebar on every list view; default sort `updated_at desc` on `AchievementModelView`, `name` asc on `ManagerModelView`; bulk action `Пересчитать очки` calls `services/scoring_service`; explicit `configure_mappers()` before view registration to fix a startup warning. Tests in `tests/test_admin_views_filters.py`. |

---

## Repo state

- Branch: `devin/1778008520-post-qa-fixes` (head of PR #79).
- All work committed and pushed.
- Latest CI on PR #79: `Quality & Tests` + `E2E Smoke (Playwright)` + `Vercel Preview Comments` passed; `Vercel` itself was canceled — known non-issue for this Flask project.

## Files touched (representative — see PR diff for full list)

- `models.py` — `__str__` per ORM model.
- `services/admin/views.py` — `column_searchable_list`, `column_filters`, `column_default_sort`, `column_formatters`, `column_labels`, custom `@action("recalculate_points", ...)`. Audit `_format_target_link` resolver.
- `services/admin/__init__.py` — explicit `configure_mappers()` before view registration.
- `blueprints/admin_api/achievements.py` — AJAX bulk-add path uses canonical icon + short title.
- `data/seed_service.py` — backfills `Season.start_year/end_year` and `AchievementType.icon_path` on fresh seed.
- `migrations/versions/b5f2a8e1c34d_backfill_season_start_end_years.py`
- `migrations/versions/c7d3e2f1a8b9_backfill_achievement_type_icon_path.py`
- `migrations/versions/d8e4f3a2b9c0_normalize_admin_achievement_title.py`
- `templates/index.html` — `Войти` hidden behind `is_authenticated`.
- `static/css/sections.css` — pill-shaped `.filters-bar` + cyan caret.
- New tests: `tests/test_model_str.py`, `tests/test_admin_views_filters.py`, `tests/test_admin_achievements.py`, `tests/integration/test_admin_api.py` (additions).

## Tests

Local + CI both green at the time of writing. 543+ tests pass. Coverage ≥ 87%
(CI gate per AGENTS.md § 5).

## E2E artefacts

- `test-plan.md` — 12 assertions across 7 tickets (committed).
- `test-report.md` — full assertion table, screenshots, recording link (committed).
- Continuous annotated recording:
  https://app.devin.ai/attachments/39149772-924e-402a-822e-79b2616222c8/rec-d0caef92-9a71-4d6f-b68a-0110d30c52c3-edited.mp4
- All 12 assertions PASS.

## Staging tunnel (only valid while this VM is alive)

- URL: `https://7b61b28494e0-tunnel-zzc1nhky.devinapps.com/`
- Admin login: `s3ifer` / `s3ifer1234`
- After this session ends the tunnel will go away. To re-test E2E later, a fresh
  Devin session needs to spin up its own Flask dev server + tunnel.

## What the user should do next

1. **Merge PR #79** into `staging` via the GitHub UI.
2. **Merge PR #78** into `main` via the GitHub UI.
3. (Optional) Re-deploy staging and production, then sanity-click the live site.
4. (Optional) Move TIK-74…TIK-80 to Done in Linear with links to the merged PRs.

## What a follow-up AI session should do (if any)

- Run the `linear-sync` skill to flip TIK-74…TIK-80 to Done after merge.
- If a new round of UX feedback comes in, the next obvious incremental wins
  are variants (c)/(d) from this round's design discussion: per-record audit
  history on the manager-edit page, custom `manager_edit.html` collapsible
  fieldsets, and "Save & Add Another" affordances inside the AJAX modal. None
  of these block production; they're just incremental polish.
- Do **not** rewrite admin to Django — the analysis and decision in this round
  was explicit: Flask-Admin gives ≥ 90% of the value at ~5% of the cost. See
  `docs/decisionLog.md` if you want the long form.

## Things that are **not** issues (stop investigating these)

- `Vercel` check on PRs being canceled — known non-issue, project is Flask.
- Slight duplication between `migrations/versions/c7d3e2f1a8b9_backfill_*.py`
  and the seed: intentional — seed handles fresh DBs, migration handles
  upgraded ones with stale data.
- `prepare_admin_form` warning at first request after restart — handled by
  `configure_mappers()` in `services/admin/__init__.py`.

## Sanity-check before merging

```bash
# from a fresh checkout of devin/1778008520-post-qa-fixes
git pull --ff-only
make setup
make check       # black + isort + flake8 + mypy + pytest
make audit-deps  # pip-audit
```

If `make check` is clean and CI on PR #79 is still green, **PR is safe to merge.**
