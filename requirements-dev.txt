# Development dependencies
flake8>=7.0.0
black>=24.0.0
isort>=5.13.0
mypy>=1.10.0
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0

# E2E smoke (tests/e2e/test_smoke.py) — run manually against a live dev
# server, not wired into the CI pytest collection. After adding/upgrading
# playwright, run `./venv/bin/playwright install chromium` once to fetch
# the bundled browser.
playwright>=1.45.0
pytest-playwright>=0.5.0

# Type stubs
types-Flask>=1.1.6
types-SQLAlchemy>=1.4.53
types-redis>=4.6.0
types-requests>=2.31.0

# Security: scan installed dependencies for known CVEs (used by `make audit-deps` + CI).
pip-audit>=2.10.0

# Project-local pre-commit hooks (.pre-commit-config.yaml). Wire up once per
# checkout via `make precommit-install`. See docs/SUPERPOWERS.md.
pre-commit>=3.5.0
